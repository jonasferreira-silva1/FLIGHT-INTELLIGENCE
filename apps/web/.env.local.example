# URL base da REST API (sem barra no final)
# Em desenvolvimento local: http://localhost:3001
# Em produção: URL do seu servidor
NEXT_PUBLIC_API_URL=http://localhost:3001

# URL do servidor WebSocket (Socket.io)
# Geralmente a mesma URL da REST API
NEXT_PUBLIC_API_WS_URL=http://localhost:3001
